using System;

namespace Fuzzy
{
    public static class DateTimeOffsetExtensions
    {
        public static Fuzzy<DateTimeOffset> Between(this Fuzzy<DateTimeOffset> fuzzy, DateTimeOffset min, TimeSpan max) => throw new NotImplementedException();
    }
}
